functiontocasom(seletorAudio){
constelemento=document.querySelector(selectorAudio);
if(elements&&elementolocalName==='audio'){
elementoplay();
}
else{
consolelog('Elemento não encontrado ou seletor inválido');
}
  }
const listaDeTeclas=documentquerySelector All('tecla')
for(let contador=O;contador<listaDeTeclas lenght;contador++){
const tecla=listaDeTeclas[contador]
const instrumento=classlist[1];
const Audio='#som_${instrumento}';
